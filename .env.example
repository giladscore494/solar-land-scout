# Gemini API (server-side only). Used for natural-language explanations.
GEMINI_API_KEY=

# NREL API key. Used for solar resource enrichment.
NREL_API_KEY=

# MapTiler / MapLibre vector tile key (client-side, public by design).
NEXT_PUBLIC_MAPTILER_KEY=
